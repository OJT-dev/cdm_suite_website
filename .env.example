# === Database: Direct Postgres for Prisma CLI and scripts ===
# Used by prisma/schema.prisma for migrations and tooling.
DATABASE_DIRECT_URL="postgresql://postgres:supersecret@localhost:5432/cdmsuite?schema=public"

# === Database: Prisma Accelerate / Data Proxy for runtime ===
# Used by lib/db.ts via `datasourceUrl` and @prisma/client/edge.
# Replace with the actual URL from Prisma Accelerate (prisma://...).
DATABASE_URL="prisma://accelerate.prisma-data.net/?api_key=YOUR_ACCELERATE_API_KEY"

# === NextAuth ===
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="a-very-long-random-string"

# === Stripe ===
STRIPE_SECRET_KEY="sk_test_YOUR_STRIPE_SECRET_KEY_HERE"
STRIPE_WEBHOOK_SECRET="whsec_YOUR_WEBHOOK_SECRET_HERE"

# === Analytics (PostHog) ===
# Match these to the keys expected in lib/analytics.ts
NEXT_PUBLIC_POSTHOG_KEY="phc_XXXXXXXXXXXXXXXXXXXXXXXX"
NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# === Reddit Conversions ===
# Match these names to app/api/analytics/reddit-conversion/route.ts and lib/analytics.ts
REDDIT_CONVERSION_TOKEN="your-reddit-conversion-token"
NEXT_PUBLIC_REDDIT_PIXEL_ID="your-reddit-pixel-id"

# === Email provider (example: Resend) ===
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# === Stripe public key (client-side) ===
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_XXXXXXXXXXXXXXXXXXXXXXXX"

# === Admin / internal app config ===
ADMIN_EMAIL="hello@example.com"
CRON_SECRET="your-cron-secret-key"

# Add any additional secrets your app already expects...
